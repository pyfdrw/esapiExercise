<UserControl x:Class="PlanReport.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlanReport.Views"
             xmlns:exPlan="clr-namespace:ExamplePlan.View"
             xmlns:doseMetric="clr-namespace:Dose_Metrics.Views"
             xmlns:dvh="clr-namespace:DVH_Reporter.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <exPlan:PlanDetailView DataContext="{Binding PlanViewModel}"></exPlan:PlanDetailView>
        <doseMetric:DoseMetricView DataContext="{Binding DoseMetricViewModel}" Grid.Row="1"></doseMetric:DoseMetricView>
        <dvh:DVHView DataContext="{Binding DVHViewModel}" Grid.Row="2"></dvh:DVHView>
    </Grid>
</UserControl>
